services:
  - type: web
    name: qwen-telegram-bot
    env: python
    buildCommand: |
      pip install -r requirements.txt
      # Model already downloaded, just verify
      ls -lh *.gguf
    startCommand: python bot.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
